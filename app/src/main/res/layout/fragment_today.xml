<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:padding="16dp"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Header con clima actual -->
    <TextView
        android:id="@+id/tvHeader"
        android:text="CDMX — Hoy"
        android:textStyle="bold"
        android:textSize="18sp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

    <!-- Bloque de clima actual (temperatura, sensación térmica y viento) -->
    <LinearLayout
        android:id="@+id/currentWeatherContainer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:paddingTop="12dp"
        android:paddingBottom="12dp">

        <!-- Temperatura principal -->
        <TextView
            android:id="@+id/tvTempMain"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="--°C"
            android:textSize="40sp"
            android:textStyle="bold"
            android:drawableStart="@drawable/wi_thermometer"
            android:drawablePadding="8dp" />

        <!-- Datos adicionales: sensación térmica y viento -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical">

            <TextView
                android:id="@+id/tvFeelsLike"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Sensación térmica: --°C"
                android:textSize="14sp"
                android:drawableStart="@drawable/wi_thermometer_exterior"
                android:drawablePadding="6dp" />

            <TextView
                android:id="@+id/tvWindSpeed"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Viento: -- km/h"
                android:textSize="14sp"
                android:drawableStart="@drawable/wi_windy"
                android:drawablePadding="6dp" />
        </LinearLayout>

    </LinearLayout>

    <!-- Lista de horas -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvHourly"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />

    <!-- Contenedor para recomendaciones -->
    <LinearLayout
        android:id="@+id/recoContainer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingTop="8dp" />

</LinearLayout>
